# Define the base URL
basename: /reactjs-dashboard

# Define the deployment branch
deploy:
  provider: pages
  skip_cleanup: true
  token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: gh-pages
